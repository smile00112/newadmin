<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Tochka Bank Payment Gateway Configuration
    |--------------------------------------------------------------------------
    |
    | This file contains the configuration for Tochka Bank payment gateway.
    |
    */

    /*
    |--------------------------------------------------------------------------
    | Server URL
    |--------------------------------------------------------------------------
    |
    | URL формы оплаты банка. По умолчанию используется тестовый/продакшн URL.
    |
    */
    'server_url' => env('TOCHKA_SERVER_URL', 'https://merch.tochka.com/woocommerce'),

    /*
    |--------------------------------------------------------------------------
    | Client ID
    |--------------------------------------------------------------------------
    |
    | Идентификатор клиента (мерчанта) из личного кабинета Точка Банка.
    |
    */
    'client_id' => env('TOCHKA_CLIENT_ID', ''),

    /*
    |--------------------------------------------------------------------------
    | Secret Key (callback verification only, not exposed in UI)
    |--------------------------------------------------------------------------
    |
    | Секретный ключ для проверки подписи callback от банка. Задаётся только в .env.
    |
    */
    'secret_key' => env('TOCHKA_SECRET_KEY', ''),

    /*
    |--------------------------------------------------------------------------
    | Webhook URL
    |--------------------------------------------------------------------------
    |
    | URL для отправки уведомлений о платежах на сторонний сервер.
    | Этот URL будет вызываться при успешной оплате.
    |
    */
    'webhook_url' => env('TOCHKA_WEBHOOK_URL', ''),

    /*
    |--------------------------------------------------------------------------
    | API Token
    |--------------------------------------------------------------------------
    |
    | Токен для аутентификации внешних API запросов на создание платежей.
    | Используется в заголовке Authorization: Bearer {token}
    |
    */
    'api_token' => env('TOCHKA_API_TOKEN', ''),

    /*
    |--------------------------------------------------------------------------
    | Minimum Amount
    |--------------------------------------------------------------------------
    |
    | Минимальная сумма платежа в рублях.
    |
    */
    'min_amount' => env('TOCHKA_MIN_AMOUNT', 1.00),

    /*
    |--------------------------------------------------------------------------
    | Service Name
    |--------------------------------------------------------------------------
    |
    | Идентификатор платежной системы, отправляемый в банк.
    |
    */
    'service_name' => env('TOCHKA_SERVICE_NAME', 'tochka_laravel_payments'),

    /*
    |--------------------------------------------------------------------------
    | Language
    |--------------------------------------------------------------------------
    |
    | Язык интерфейса оплаты (ru или en).
    |
    */
    'lang' => env('TOCHKA_LANG', 'ru'),

    /*
    |--------------------------------------------------------------------------
    | Signature Algorithms
    |--------------------------------------------------------------------------
    |
    | Алгоритмы хеширования для подписей:
    | - request: алгоритм для подписи исходящих запросов (форма оплаты)
    | - callback: алгоритм для проверки подписи входящих callback
    |
    */
    'signature_algorithms' => [
        'request' => 'sha256',
        'callback' => 'md5',
    ],
];
